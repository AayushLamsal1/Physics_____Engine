g#include "Ground.hpp"

// All functions are already inline in header.
// If you want, you can move definitions here:

/*
Ground::Ground(float x, float y, float width, float height)
{
    rect.setSize({ width, height });
    rect.setPosition(x, y);
    rect.setFillColor(sf::Color(180, 180, 180));
}

void Ground::draw(sf::RenderWindow& window)
{
    window.draw(rect);
}

float Ground::getY() const
{
    return rect.getPosition().y;
}

sf::FloatRect Ground::getBounds() const
{
    return rect.getGlobalBounds();
}
*/
